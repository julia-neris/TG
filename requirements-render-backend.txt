# =====================================================================
# Dependências otimizadas para deploy no Render (sem GPU)
# Usa versões CPU-only do PyTorch e TensorFlow para reduzir:
#   - Tamanho do build (~2GB a menos vs versão CUDA)
#   - Tempo de build
#   - Uso de memória em runtime
# =====================================================================

# PyTorch CPU-only (versão CUDA = ~2.5GB | versão CPU = ~200MB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchaudio>=2.0.0

# TensorFlow CPU-only (versão padrão = ~2GB | tensorflow-cpu = ~400MB)
tensorflow-cpu>=2.13.0

# FastAPI + servidor ASGI
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
pydantic>=2.0.0

# Processamento de áudio
librosa>=0.10.0
numpy>=1.24.3
scipy>=1.11.1
soundfile>=0.12.1
numba>=0.57.1
sounddevice>=0.4.5

# Separação de instrumentos
demucs>=4.0.0

# Utilitários
matplotlib>=3.7.2
scikit-learn>=1.3.0
